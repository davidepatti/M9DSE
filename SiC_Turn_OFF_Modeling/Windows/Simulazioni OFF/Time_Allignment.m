if (I_L==10 && R_gH==2.2 && R_gL==2.2)
    % Ritardo temporale delle forme d'onda del Low Side 
    Dt_exp_sim=10e-8;
    % Ritardo temporale tra le forme d'onda di High Side e Low Side 
    Dt_HS_LS=0e-8;
    % Definizione del vettore temporale di Low Side
    t_exp_LS=0:1/6.25e9:400e-9-1/6.25e9;
end

if (I_L==30 && R_gH==2.2 && R_gL==2.2)
    % Ritardo temporale delle forme d'onda del Low Side 
    Dt_exp_sim=10e-8;
    % Ritardo temporale tra le forme d'onda di High Side e Low Side 
    Dt_HS_LS=0e-8;
    % Definizione del vettore temporale di Low Side
    t_exp_LS=0:1/6.25e9:400e-9-1/6.25e9;
end

if (I_L==50 && R_gH==2.2 && R_gL==2.2)
    % Ritardo temporale delle forme d'onda del Low Side 
    Dt_exp_sim=10e-8;
    % Ritardo temporale tra le forme d'onda di High Side e Low Side 
    Dt_HS_LS=0e-9;
    % Definizione del vettore temporale di Low Side
    t_exp_LS=0:1/6.25e9:400e-9-1/6.25e9;
end

if (I_L==70 && R_gH==2.2 && R_gL==2.2)
    % Ritardo temporale delle forme d'onda del Low Side 
    Dt_exp_sim=10e-8;
    % Ritardo temporale tra le forme d'onda di High Side e Low Side 
    Dt_HS_LS=0e-8;
    % Definizione del vettore temporale di Low Side
    t_exp_LS=0:1/6.25e9:400e-9-1/6.25e9;
end

if (I_L==10 && R_gH==2.2 && R_gL==8.2)
    % Ritardo temporale delle forme d'onda del Low Side 
    Dt_exp_sim=10e-8;
    % Ritardo temporale tra le forme d'onda di High Side e Low Side 
    Dt_HS_LS=0e-8;
    % Definizione del vettore temporale di Low Side
    t_exp_LS=0:1/6.25e9:400e-9-1/6.25e9;
end

if (I_L==30 && R_gH==2.2 && R_gL==8.2)
    % Ritardo temporale delle forme d'onda del Low Side 
    Dt_exp_sim=10e-8;
    % Ritardo temporale tra le forme d'onda di High Side e Low Side 
    Dt_HS_LS=0e-8;
    % Definizione del vettore temporale di Low Side
    t_exp_LS=0:1/6.25e9:400e-9-1/6.25e9;
end

if (I_L==50 && R_gH==2.2 && R_gL==8.2)
    % Ritardo temporale delle forme d'onda del Low Side 
    Dt_exp_sim=10e-8;
    % Ritardo temporale tra le forme d'onda di High Side e Low Side 
    Dt_HS_LS=0e-9;
    % Definizione del vettore temporale di Low Side
    t_exp_LS=0:1/6.25e9:400e-9-1/6.25e9;
end

if (I_L==70 && R_gH==2.2 && R_gL==8.2)
    % Ritardo temporale delle forme d'onda del Low Side 
    Dt_exp_sim=10e-8;
    % Ritardo temporale tra le forme d'onda di High Side e Low Side 
    Dt_HS_LS=0e-8;
    % Definizione del vettore temporale di Low Side
    t_exp_LS=0:1/6.25e9:400e-9-1/6.25e9;
end

if (R_gH==8.2 && R_gL==8.2)
    % Ritardo temporale delle forme d'onda del Low Side 
    Dt_exp_sim=6e-8;
    % Ritardo temporale tra le forme d'onda di High Side e Low Side 
    Dt_HS_LS=0e-8;
    % Definizione del vettore temporale di Low Side
    t_exp_LS=0:1/6.25e9:400e-9-1/6.25e9;
end

if ( I_L==30 && R_gH==8.2 && R_gL==8.2)
    % Ritardo temporale delle forme d'onda del Low Side 
    Dt_exp_sim=5e-8;
    % Ritardo temporale tra le forme d'onda di High Side e Low Side 
    Dt_HS_LS=0e-8;
    % Definizione del vettore temporale di Low Side
    t_exp_LS=0:1/6.25e9:400e-9-1/6.25e9;
end

if ( I_L==50 && R_gH==8.2 && R_gL==8.2)
    % Ritardo temporale delle forme d'onda del Low Side 
    Dt_exp_sim=5e-8;
    % Ritardo temporale tra le forme d'onda di High Side e Low Side 
    Dt_HS_LS=0e-8;
    % Definizione del vettore temporale di Low Side
    t_exp_LS=0:1/6.25e9:400e-9-1/6.25e9;
end

if ( I_L==70 && R_gH==8.2 && R_gL==8.2)
    % Ritardo temporale delle forme d'onda del Low Side 
    Dt_exp_sim=5.5e-8;
    % Ritardo temporale tra le forme d'onda di High Side e Low Side 
    Dt_HS_LS=0e-8;
    % Definizione del vettore temporale di Low Side
    t_exp_LS=0:1/6.25e9:400e-9-1/6.25e9;
end

if ( I_L==10 && R_gH==8.2 && R_gL==2.2)
    % Ritardo temporale delle forme d'onda del Low Side 
    Dt_exp_sim=5e-8;
    % Ritardo temporale tra le forme d'onda di High Side e Low Side 
    Dt_HS_LS=0e-8;
    % Definizione del vettore temporale di Low Side
    t_exp_LS=0:1/6.25e9:400e-9-1/6.25e9;
end

if ( I_L==30 && R_gH==8.2 && R_gL==2.2)
    % Ritardo temporale delle forme d'onda del Low Side 
    Dt_exp_sim=5e-8;
    % Ritardo temporale tra le forme d'onda di High Side e Low Side 
    Dt_HS_LS=0e-8;
    % Definizione del vettore temporale di Low Side
    t_exp_LS=0:1/6.25e9:400e-9-1/6.25e9;
end

if ( I_L==50 && R_gH==8.2 && R_gL==2.2)
    % Ritardo temporale delle forme d'onda del Low Side 
    Dt_exp_sim=5.5e-8;
    % Ritardo temporale tra le forme d'onda di High Side e Low Side 
    Dt_HS_LS=0e-8;
    % Definizione del vettore temporale di Low Side
    t_exp_LS=0:1/6.25e9:400e-9-1/6.25e9;
end

if ( I_L==70 && R_gH==8.2 && R_gL==2.2)
    % Ritardo temporale delle forme d'onda del Low Side 
    Dt_exp_sim=5.5e-8;
    % Ritardo temporale tra le forme d'onda di High Side e Low Side 
    Dt_HS_LS=0e-8;
    % Definizione del vettore temporale di Low Side
    t_exp_LS=0:1/6.25e9:400e-9-1/6.25e9;
end

if ( I_L==10 && R_gH==20 && R_gL==2.2)
    % Ritardo temporale delle forme d'onda del Low Side 
    Dt_exp_sim=10e-8;
    % Ritardo temporale tra le forme d'onda di High Side e Low Side 
    Dt_HS_LS=0e-8;
    % Definizione del vettore temporale di Low Side
    t_exp_LS=0:1/6.25e9:800e-9-1/6.25e9;
end

if ( I_L==30 && R_gH==20 && R_gL==2.2)
    % Ritardo temporale delle forme d'onda del Low Side 
    Dt_exp_sim=12e-8;
    % Ritardo temporale tra le forme d'onda di High Side e Low Side 
    Dt_HS_LS=0e-8;
    % Definizione del vettore temporale di Low Side
    t_exp_LS=0:1/6.25e9:800e-9-1/6.25e9;
end

if ( I_L==50 && R_gH==20 && R_gL==2.2)
    % Ritardo temporale delle forme d'onda del Low Side 
    Dt_exp_sim=12e-8;
    % Ritardo temporale tra le forme d'onda di High Side e Low Side 
    Dt_HS_LS=0e-8;
    % Definizione del vettore temporale di Low Side
    t_exp_LS=0:1/6.25e9:800e-9-1/6.25e9;
end

if ( I_L==70 && R_gH==20 && R_gL==2.2)
    % Ritardo temporale delle forme d'onda del Low Side 
    Dt_exp_sim=12e-8;
    % Ritardo temporale tra le forme d'onda di High Side e Low Side 
    Dt_HS_LS=0e-8;
    % Definizione del vettore temporale di Low Side
    t_exp_LS=0:1/6.25e9:800e-9-1/6.25e9;
end

if ( I_L==10 && R_gH==20 && R_gL==8.2)
    % Ritardo temporale delle forme d'onda del Low Side 
    Dt_exp_sim=10e-8;
    % Ritardo temporale tra le forme d'onda di High Side e Low Side 
    Dt_HS_LS=0e-8;
    % Definizione del vettore temporale di Low Side
    t_exp_LS=0:1/6.25e9:800e-9-1/6.25e9;
end

if ( I_L==30 && R_gH==20 && R_gL==8.2)
    % Ritardo temporale delle forme d'onda del Low Side 
    Dt_exp_sim=13e-8;
    % Ritardo temporale tra le forme d'onda di High Side e Low Side 
    Dt_HS_LS=0e-8;
    % Definizione del vettore temporale di Low Side
    t_exp_LS=0:1/6.25e9:800e-9-1/6.25e9;
end

if ( I_L==50 && R_gH==20 && R_gL==8.2)
    % Ritardo temporale delle forme d'onda del Low Side 
    Dt_exp_sim=12e-8;
    % Ritardo temporale tra le forme d'onda di High Side e Low Side 
    Dt_HS_LS=0e-8;
    % Definizione del vettore temporale di Low Side
    t_exp_LS=0:1/6.25e9:800e-9-1/6.25e9;
end

if ( I_L==70 && R_gH==20 && R_gL==8.2)
    % Ritardo temporale delle forme d'onda del Low Side 
    Dt_exp_sim=12e-8;
    % Ritardo temporale tra le forme d'onda di High Side e Low Side 
    Dt_HS_LS=0e-8;
    % Definizione del vettore temporale di Low Side
    t_exp_LS=0:1/6.25e9:800e-9-1/6.25e9;
end
